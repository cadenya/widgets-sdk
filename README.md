# Cadenya Widgets SDK

The official TypeScript SDK for the Cadenya Widgets API (`@cadenya/widgets`).

The first release is on its way — this repository receives its content
through Cadenya's automated release flow.
